'use strict';

var PiecePlacementParser = require('../piece-placement-parser'),
  assert = require('chai').assert;

describe('PiecePlacementParser', function () {
  describe('#parse()', function () {
    it('throw error if ranks number not equal to 8');
  });
});
