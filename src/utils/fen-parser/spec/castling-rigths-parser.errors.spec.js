'use strict';

var CastlingRightsParser = require('../castling-rights-parser');

describe('CastlingRightsParser', function () {
  describe('#parse()', function () {
    it('throw error for invalid castling rights token');
    it('throw error for repeated castling token');
    it('throw error for wrong castling token order');
  });
});
