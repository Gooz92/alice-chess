'use strict';

var FenParser = require('../fen-parser');

describe('FenParser', function () {
  describe('#parse()', function () {
    it('throw error for invalid color token');
    it('throw error for invalid e.p. target square');
    // TODO need more
  });
});
