'use strict';

var assert = require('chai').assert,
  Chess = require('../chess');

describe('Move execution', function () {
  describe('Pawn silent move', function () {
    var chess = Chess.createStartPosition();
  });
});
